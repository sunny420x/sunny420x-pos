<html>
    <head>
        <title>รายการส่วนลด | SunnyPOS</title>
        <%-include('include/head')%>
    </head>
    <body>
        <%
        helper = new Helper()
        %>
        <div class="d-flex">
            <%-include('include/sidebar')%>
            <div class="container overflow-hidden">
                <!-- <nav class="navbar">
                    <div class="navbar-brand"><a class="px-4 py-2 text-decoration-none text-black" onclick="history.back()">
                        <i class="fa-solid fa-chevron-left me-4"></i> รายชื่อลูกค้า</a></div>
                </nav> -->
                <%-include('include/navigator')%>
                <div class="container my-4">
                    <div class="card p-4">
                        <h2 class="mb-4"><i class="fa-solid fa-percentage me-1"></i> รายการส่วนลด <button type="button" class="btn btn-primary btn-sm" onclick="openLink('/addDiscount')"><i class="fa-solid fa-plus me-1"></i> เพิ่มส่วนลด</button></h2>
                        <div class="row">
                            <div class="table-responsive col">
                                <table class="table">
                                    <thead class="table-light text-center">
                                        <tr>
                                            <th>#</th>
                                            <th>โค้ดส่วนลด</th>
                                            <th>(%) ส่วนลดร้อยละ</th>
                                            <th>สถานะ</th>
                                            <th>จัดการ</th>
                                        </tr>
                                    </thead>
                                    <% discounts.forEach((row) => { %>
                                        <tbody class="text-center">
                                            <tr>
                                                <td><%=row.id%></td>
                                                <td><%=row.code%></td>
                                                <td><%=row.amount%> %</td>
                                                <td><%if(row.status == 0) {%><span class="badge bg-success">ยังไม่ถูกใช้งาน</span><%} else {%><span class="badge bg-danger">ใช้งานแล้ว</span><%}%></td>
                                                <td>
                                                    <button class="btn btn-primary" onclick="openLink('/discounts/<%=row.id%>')"><i class="fa-solid fa-folder"></i></button>
                                                    <button class="btn btn-danger" onclick="
                                                    Swal.fire({
                                                    title: 'คุณต้องการลบข้อมูลนี้หรือไม่?',
                                                    text: 'การลบจะไม่สามารถย้อนกลับได้',
                                                    icon: 'warning',
                                                    showCancelButton: true,
                                                    confirmButtonColor: '#3085d6',
                                                    cancelButtonColor: '#d33',
                                                    confirmButtonText: 'ยืนยัน',
                                                    cancelButtonText: 'ย้อนกลับ',
                                                    }).then((result) => {
                                                    if (result.isConfirmed) {
                                                        openLink('/deleteDiscount/<%=row.id%>')
                                                    }
                                                    });
                                                    "><i class="fa-solid fa-trash"></i></button>
                                                </td>
                                            </tr>
                                        </tbody>
                                    <%})%>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <%-include('include/footer')%>
    </body>
</html>